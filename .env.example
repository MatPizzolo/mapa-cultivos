# Completar y renombrar a .env — la key JSON vive FUERA del repo.
GEE_SERVICE_ACCOUNT=
GEE_KEY_PATH=
GCS_TILES_BUCKET=
# Campaña del MNC contra la que se muestrea. Es un parámetro, no una constante:
# si la 2024/25 no está publicada en GeoINTA, se cae a la última disponible.
MNC_CAMPANIA=2024-25
